[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/node-service.Dockerfile"
watchPatterns = [
  "/package.json",
  "/package-lock.json",
  "/docker/node-service.Dockerfile",
  "/packages/shared/**",
  "/railway/user-service.toml",
  "/services/user-service/**"
]

[deploy]
startCommand = "node services/user-service/src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
